# ===== 로컬 실행용 비밀 설정 파일 =====
# 이 파일은 .gitignore 에 등록되어 있어 GitHub에 올라가지 않습니다.
# (인증키가 외부에 노출되지 않도록 보호하는 용도)
#
# 로컬에서 `streamlit run app.py` 로 실행하면 app.py 가 이 값을 자동으로 읽습니다.
#
# 배포(Streamlit Community Cloud)할 때는 이 파일을 올리는 게 아니라,
# 사이트의 [App settings → Secrets] 칸에 아래 한 줄을 그대로 붙여 넣으면 됩니다.

SERVICE_KEY = "a259c8267267163027ba8e9a1beec3a2973d0f1ad8e7a885d2dc8b9f46be0fde"
